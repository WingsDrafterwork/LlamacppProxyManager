[pytest]
# pytest.ini configuration file

# Add options here
addopts = -v --tb=short

# Test paths
testpaths = tests

# Configure the test marker
markers =
    slow: mark test as slow to run
    smoke: mark test as smoke test
